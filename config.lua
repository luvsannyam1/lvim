-- Load all plugins
require("cryout.plugins")

-- Load Treesitter config
require("cryout.treesitter")

-- Load LSP config (linters, formatters, diagnostics)
require("cryout.lsp")

-- Load MISC config
require("cryout.misc")

-- Load KeyMap config
require("cryout.keymap")
